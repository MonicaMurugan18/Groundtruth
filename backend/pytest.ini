[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
markers =
    live: requires OPENAI_API_KEY and makes real LLM calls
    moss: requires Moss credentials
    livekit: requires LiveKit credentials
